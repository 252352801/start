import { EventEmitter, SimpleChanges, OnInit, OnChanges, ElementRef } from '@angular/core';
export declare class ModalComponent implements OnInit, OnChanges {
    private elemRef;
    visible: boolean;
    visibleChange: EventEmitter<any>;
    overflow: boolean;
    animated: boolean;
    size: string;
    isRender: boolean;
    isShow: boolean;
    outClickClose: boolean;
    showCloseButton: boolean;
    constructor(elemRef: ElementRef);
    ngOnInit(): void;
    /**
     * 背景点击处理
     */
    private bgClickAction(ev);
    private close();
    ngOnChanges(changes: SimpleChanges): void;
    /**
     * 初始化modal-body的高度/限高
     */
    private initBodyStyle();
    /**
     * 为元素添加一个类
     * @param elem
     * @param className
     */
    private addClass(elem, className);
    /**
     * 删除某个类
     * @param elem
     * @param className
     */
    private removeClass(elem, className);
}
export declare class ModalHeaderComponent extends ModalComponent {
}
export declare class ModalBodyComponent {
    constructor();
}
export declare class ModalFooterComponent {
    constructor();
}
