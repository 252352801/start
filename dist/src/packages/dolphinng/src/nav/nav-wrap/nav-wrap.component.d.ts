import { OnInit, ElementRef } from '@angular/core';
export declare class NavWrapComponent implements OnInit {
    private elemRef;
    test: string;
    constructor(elemRef: ElementRef);
    ngOnInit(): void;
}
